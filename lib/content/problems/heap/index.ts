/**
 * heap Problems
 * 6 problems total
 */

import { last_stone_weightProblem } from './last-stone-weight';
import { kth_largest_element_streamProblem } from './kth-largest-element-stream';
import { relative_ranksProblem } from './relative-ranks';
import { kth_largest_element_arrayProblem } from './kth-largest-element-array';
import { k_closest_pointsProblem } from './k-closest-points';
import { task_schedulerProblem } from './task-scheduler';

export const heapProblems = [
  last_stone_weightProblem, // 1. Last Stone Weight
  kth_largest_element_streamProblem, // 2. Kth Largest Element in a Stream
  relative_ranksProblem, // 3. Relative Ranks
  kth_largest_element_arrayProblem, // 4. Kth Largest Element in an Array
  k_closest_pointsProblem, // 5. K Closest Points to Origin
  task_schedulerProblem, // 6. Task Scheduler
];
