/**
 * time-space-complexity Problems
 * 5 problems total
 */

import { pivot_indexProblem } from './pivot-index';
import { first_unique_charProblem } from './first-unique-char';
import { duplicate_numberProblem } from './duplicate-number';
import { array_partitionProblem } from './array-partition';
import { valid_numberProblem } from './valid-number';

export const timeSpaceComplexityProblems = [
  pivot_indexProblem, // 2. Find Pivot Index
  first_unique_charProblem, // 3. First Unique Character in a String
  duplicate_numberProblem, // 4. Find the Duplicate Number
  array_partitionProblem, // 5. Array Partition
  valid_numberProblem, // 6. Valid Number
];
