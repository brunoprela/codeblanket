# Environment variables for CodeBlanket

# Next.js will automatically load these from .env.local for local development
# For Vercel deployment, add these in the Vercel dashboard under Settings > Environment Variables

# Node environment (automatically set by Vercel)
# NODE_ENV=production

# Add any API keys or secrets here as needed
# NEXT_PUBLIC_API_URL=https://api.example.com

